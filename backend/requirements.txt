# Minimal production dependency set for the CodePilot AI backend.
#
# This list mirrors backend/pyproject.toml and contains ONLY packages the
# backend actually imports. It intentionally excludes heavyweight ML/Jupyter/
# data-science packages (tensorflow, keras, jupyter, matplotlib, scipy,
# scikit-learn, pandas, ...) that are not used by this application and would
# bloat the 512 MB runtime image / slow down every deploy.
#
# The Docker build installs via `uv sync --frozen --no-dev` from pyproject.toml
# + uv.lock; requirements.txt is kept in sync for pip-based local/dev installs.
alembic==1.19.0
fastapi==0.141.1
fastembed==0.8.0
gitpython==3.1.58
groq==1.6.0
psycopg[binary]==3.3.4
pydantic-settings==2.15.0
qdrant-client==1.19.0
sqlalchemy==2.0.51
tree-sitter==0.26.0
tree-sitter-language-pack==1.14.3
uvicorn==0.52.1